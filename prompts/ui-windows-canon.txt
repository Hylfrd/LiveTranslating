Create a high-fidelity 16:9 mockup of a polished Windows desktop utility called LiveTranslating. Show a large white frameless main window and a detached compact subtitle window. This is the category-standard direction executed exceptionally well.

The main app uses a familiar split view: compact left navigation and settings, a wide central live transcript, and a slim right details pane. It includes System audio and Microphone toggles and meters, microphone device selection, source and target language, model selection with DeepSeek V4 Flash active, recording, delayed review, terminology count, live bilingual subtitles, recent logs, latency and error states. White ground, near-black text, neutral gray dividers, restrained Windows blue for focus, red only for recording/error. Dense, calm, accessible, and keyboard-friendly. Avoid card soup; use native command-bar rhythm and full-width sections.

The small subtitle window is frameless with a slightly translucent gray background and three recent bilingual lines. Exact chrome: top left only expand; top right only minus and X; nothing else. It is a normal taskbar-visible window. Use Simplified Chinese UI labels and the exact sample Chinese line "逻辑回归将 θᵀx 输入 Sigmoid 函数，使输出限定在 0 到 1 之间。" No gradients, no marketing layout, no illustration, no purple, no excessive rounded panels. Realistic premium Windows application screenshot.
